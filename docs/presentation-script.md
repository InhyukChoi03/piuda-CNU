# 피우다 2.0 8분 발표 시나리오

이 문서는 현재 저장소의 실제 구현을 기준으로 한 기능·기술 중심 발표 대본이다. 전체 시간은 **정확히 8분(480초)**이다. `발표 멘트`의 인용문만 실제로 말하고, 대괄호와 뒤쪽의 구현 근거·실패 대안·체크리스트는 진행용이다.

## 역할과 화면

| 담당 | 역할 | 발표 중 유지할 화면 |
| --- | --- | --- |
| A · 발표자/제어실 | 발표, 시나리오 전환, 시간 관리 | `http://CNU.local:8080/demo` |
| B · 사용자 | Raspberry Pi 사용자 동작, AI 질문, 보호자 알림 요청 | Pi 키오스크 `http://127.0.0.1:8080/?kiosk=1` |
| C · 보호자 | 알림 확인, 보호자 대시보드 | `http://CNU.local:8080/caregiver` |

보호자 화면은 발표 전에 PIN `3017`로 로그인한다. 세 화면은 같은 네트워크에 연결하고, 발표 중에는 모두 전면에 둔다.

## 정확히 8분 타임라인

| 구간 | 길이 | 핵심 기능 | A · 발표자/제어실 | B · 사용자 | C · 보호자 |
| --- | ---: | --- | --- | --- | --- |
| 00:00–00:35 | 35초 | 문제와 해결 방향 | 발표 시작, 사용자 화면을 가리킴 | 기본 사용자 화면 유지 | 대시보드 유지 |
| 00:35–01:15 | 40초 | 전체 데이터 흐름 | 구조 설명 | 사용자 화면 유지 | 대시보드 유지 |
| 01:15–02:05 | 50초 | 반복 일정과 완료 기록 | `약 먹을 시간` 실행 | `아침 약 복용`의 `완료했어요` 클릭 | 일정 변화 확인 |
| 02:05–03:05 | 60초 | PIR·CSI와 설명 가능한 점수 | `넘어짐 의심` 실행 | 50점·주의 안내 확인 | 감점 근거·신뢰도 확인 |
| 03:05–04:10 | 65초 | 사용자 선확인 후 단계적 알림 | `낮 시간 3시간 무활동` 실행 | 30초 동안 응답하지 않음 | 처음에는 알림이 없고, 타임아웃 뒤 알림 발생 확인 |
| 04:10–04:55 | 45초 | 보호자 대시보드 | 기술 설명 | 화면 유지 | 일정·점수·근거·센서·알림을 보여 주고 `확인했습니다` 클릭 |
| 04:55–06:00 | 65초 | 로컬 AI·기억·안전 폴백 | 기술 설명 | 준비한 두 질문을 순서대로 전송 | 화면 유지 |
| 06:00–07:30 | 90초 | 사용자 직접 보호자 알림 | `보호자 알림 요청` 장면과 기술 설명 | 실제 `보호자에게 알림 보내기` 클릭 | 큰 팝업·소리·진동 확인 후 처리 |
| 07:30–08:00 | 30초 | 회복과 결론 | `활동 재확인` 실행, 결론 | 100점·안심 복귀 확인 | 회복 알림 확인 |

**시간 합계: 35 + 40 + 50 + 60 + 65 + 45 + 65 + 90 + 30 = 480초 = 8분**

### 시간 운영 원칙

- A는 00:35, 01:15, 02:05, 03:05, 04:10, 04:55, 06:00, 07:30에 다음 구간으로 반드시 넘어간다.
- 화면 반영은 최대 한 번의 2초 갱신 주기만 기다린다. 늦으면 아래 실패 대비 플랜을 사용하고 설명을 계속한다.
- AI 첫 응답이 20초 안에 없으면 두 번째 기억 질문은 생략한다. 직접 알림은 한 번의 2초 갱신 주기만 기다리고, 07:20에는 확인 처리를 끝내 07:30 결론 화면을 준비한다.

## 실제 발표 대본과 조작 큐

### 00:00–00:35 · 문제와 해결 방향

**조작**

- [A는 사용자 화면의 큰 글씨 체크리스트와 안심 지수를 가리킨다.]
- [B와 C는 조작하지 않는다.]

**발표 멘트**

> 안녕하세요. 피우다는 초기 치매 환자와 경증 장애인이 일상을 스스로 수행하도록 돕고, 보호자는 꼭 필요한 순간에만 확인하도록 만든 Raspberry Pi 기반 자립생활지원 시스템입니다. 카메라로 생활을 촬영하는 대신 일정 수행 정보와 PIR, Wi-Fi CSI 생활 신호를 사용합니다. 화면의 점수는 의료 진단이 아니라 보호자가 확인 순서를 정하는 보조 정보입니다.

### 00:35–01:15 · 전체 데이터 흐름

**조작**

- [A는 사용자 화면, CSI 송신·수신 ESP32 두 대, 보호자 스마트폰을 차례로 가리킨다.]

**발표 멘트**

> 전체 흐름은 단순합니다. 한 ESP32가 초당 50개의 측정 패킷을 만들고, 다른 ESP32가 PIR과 Wi-Fi CSI 변화를 분석해 센서별 API 키와 함께 Raspberry Pi의 Flask REST API로 전송합니다. Pi는 반복 일정을 오늘의 체크리스트로 만들고, 일정과 센서 이벤트를 함께 점수화합니다. 결과와 알림은 SQLite에 저장되고 사용자 화면과 PIN 보호 보호자 화면이 2초 주기로 동기화됩니다. SQLite에는 외래 키와 WAL 모드를 적용해 여러 화면의 동시 조회와 기록을 처리했습니다.

### 01:15–02:05 · 반복 일정과 완료 기록

**조작**

1. [A가 제어실에서 `약 먹을 시간`을 실행한다.]
2. [약 2초 뒤 B가 강조된 `아침 약 복용`의 `완료했어요`를 누른다.]
3. [C는 보호자 화면의 일정 완료 상태가 바뀌는 것을 보여 준다.]

**발표 멘트**

> 보호자는 식사, 복약, 외출 같은 반복 일정을 등록할 수 있습니다. 서버는 요일을 비트마스크로 저장하고 오늘 날짜와 시간을 결합해 실제 수행 항목을 만듭니다. 같은 날짜의 일정이 중복 생성되지 않도록 일정 ID와 예정 시각을 유일키로 두었습니다. 지금 사용자가 완료 버튼을 누르면 상태와 완료 시각이 저장되고 점수를 즉시 다시 계산합니다. 큰 버튼, 짧은 안내, 완료 음성은 사용자가 혼자서도 다음 행동을 이해하도록 설계한 부분입니다.

### 02:05–03:05 · 비접촉 센서와 설명 가능한 점수

**조작**

1. [A가 제어실에서 `넘어짐 의심`을 실행한다.]
2. [B는 사용자 화면의 `50점 · 주의`를 보여 준다.]
3. [C는 보호자 화면에서 `Wi-Fi CSI 넘어짐 의심`, `신뢰도 91%`, `-50` 근거를 가리킨다.]

**발표 멘트**

> 송신 ESP32는 같은 2.4기가헤르츠 채널에서 초당 50개의 ESP-NOW 측정 패킷을 보냅니다. 수신 ESP32는 PIR 입력을 250밀리초마다 확인하고, Wi-Fi CSI의 I와 Q 값으로 신호 크기를 계산합니다. 초기 250개 패킷으로 기준선을 만든 뒤 변화율이 설정 임계값을 연속으로 넘으면 움직임 또는 넘어짐 의심 이벤트를 큐에 넣어 전송합니다. 서버의 실제 점수 엔진은 최근 30분 안에 들어온 신뢰도 0.65 이상의 CSI 넘어짐 신호를 50점 감점합니다. 그래서 지금 100점에서 50점이 되어 ‘주의’로 바뀌었고, 보호자는 결과뿐 아니라 신뢰도와 감점 이유까지 확인할 수 있습니다. 이번 발표에서는 실제 넘어짐을 만들지 않고 제어실이 같은 센서 조건을 재현했습니다.

### 03:05–04:10 · 사용자에게 먼저 묻고 필요한 때만 알림

**조작**

1. [03:05에 A가 `낮 시간 3시간 무활동`을 실행한다.]
2. [B는 확인 팝업이 뜨면 아무 버튼도 누르지 않는다.]
3. [C는 처음에는 새 보호자 알림이 없다는 것을 보여 준다.]
4. [약 03:37에 30초 카운트다운이 끝나면 C가 새 확인 알림을 보여 준다.]
5. [03:42까지 자동 전환되지 않으면 A가 `확인에 응답 없음`을 직접 실행하고 04:10에 다음 구간으로 넘어간다.]

**발표 멘트**

> 움직임이 없다는 이유만으로 보호자에게 바로 경보를 보내면 쉬고 있는 상황도 위험으로 오인할 수 있습니다. 피우다는 먼저 사용자 화면에 ‘지금 문제가 있나요?’라고 묻고 30초 동안 응답을 기다립니다. ‘괜찮아요’를 누르면 움직임을 다시 확인한 안심 장면으로 돌아가고 보호자 알림은 만들지 않습니다. 지금은 차이를 보여 드리기 위해 응답하지 않겠습니다. 브라우저가 활성 시각을 기준으로 매초 남은 시간을 계산하고, 30초가 지나면 로컬 wellness API에 timeout을 보냅니다. 이제서야 건강 점수가 30점으로 바뀌고 보호자에게 확인 요청이 생성됩니다. 이 선확인 흐름은 현재 발표 모드의 재현 가능한 장면으로 연결되어 있습니다.

### 04:10–04:55 · 보호자 대시보드와 인증

**조작**

1. [C가 건강 점수, 일정 진행, 감점 근거, 최근 센서 기록, 미확인 알림을 위에서 아래로 짚는다.]
2. [C가 새 알림의 `확인했습니다` 또는 `처리 확인`을 누른다.]

**발표 멘트**

> 보호자 화면은 오늘 일정, 건강 점수, 감점 근거, 센서 연결 상태, 최근 이벤트와 알림을 한 번에 조회합니다. 화면은 2초마다 갱신되고 새 위험 알림은 큰 팝업과 소리로 표시됩니다. 확인 버튼을 누르면 처리 시각이 기록됩니다. 보호자 PIN은 Werkzeug 해시로 저장하고, 로그인 토큰과 센서 API 키도 원문이 아니라 SHA-256 해시만 데이터베이스에 보관합니다. 대시보드의 변경·조회 API는 보호자 세션이나 bearer token을 요구합니다.

### 04:55–06:00 · 로컬 AI, 대화 기억, 안전 폴백

**조작**

1. [04:55에 B가 미리 복사한 `오늘 보리차를 마셨어요. 잘했죠?`를 붙여 넣고 보낸다.]
2. [응답을 기다리는 동안 A가 아래 멘트를 이어 간다.]
3. [응답 후 B가 `제가 아까 무엇을 마셨죠?`를 보내고, 답변의 `보리차`를 보여 준다.]
4. [05:15까지 첫 응답이 없으면 요청을 중복 전송하지 말고 두 번째 기억 질문을 생략한다. 설명은 계속하고 06:00에 알림 구간으로 넘어간다.]

**발표 멘트**

> 생활 도우미에는 현재 점수와 오늘 일정, 최근 대화를 함께 제공합니다. 일반 질문은 Raspberry Pi의 Ollama에서 한국어 특화 HyperCLOVA X SEED 1.5B Q4 모델로 처리하고, 최근 대화는 SQLite에 저장해 다음 응답에 반영합니다. 반면 ‘다음 할 일’, 특정 시간의 일정, 남은 일정, 119가 필요한 긴급 표현은 모델을 기다리지 않고 규칙 기반으로 즉시 답합니다. 모델이 지연되거나 실패해도 긴급 안전 문구 또는 다음 일정으로 폴백합니다. 답변은 길이와 마크다운을 정리한 뒤 Pi에서 한국어 음성으로 읽습니다. 두 번째 질문은 저장된 사용자 발화를 찾아 ‘보리차’라고 답하므로 기억을 추측하지 않습니다.

### 06:00–07:30 · 사용자 직접 보호자 알림

**조작**

1. [06:00에 A가 제어실에서 `보호자 알림 요청` 장면을 실행해 같은 결과를 먼저 보여 준다.]
2. [C가 보호자 화면의 큰 확인 팝업과 미확인 알림 증가를 보여 주고 `확인했습니다`를 누른다.]
3. [B가 사용자 화면 상단의 실제 `보호자에게 알림 보내기` 버튼을 누른다.]
4. [사용자 화면의 `알림 전송 완료`와 음성 안내를 확인한다.]
5. [약 2초 안에 C의 스마트폰에 다시 확인 팝업이 뜨는 것을 보여 주되 아직 처리하지 않는다.]
6. [B가 버튼을 곧바로 한 번 더 눌러 서버의 30초 중복 방지 안내를 보여 준 뒤 C가 확인 처리한다.]

**발표 멘트**

> 사용자는 위험 점수와 무관하게 필요할 때 직접 보호자 확인을 요청할 수 있습니다. 버튼을 누르면 같은 Wi-Fi에서만 허용되는 REST API가 위험 단계의 알림을 SQLite에 기록합니다. 보호자 화면은 2초마다 알림을 조회하고 새 항목을 큰 팝업, 알림음, 진동으로 표시합니다. 보호자가 확인을 누르면 처리 시각을 저장해 미확인 수치에서 제외합니다. 같은 요청을 연속으로 눌러 알림이 쌓이지 않도록, 미확인 직접 요청은 30초 동안 기존 항목을 재사용합니다. 카카오 토큰이 설정되어 있으면 새로 생성된 첫 요청만 카카오 알림으로도 전송합니다.

### 07:30–08:00 · 회복과 결론

**조작**

1. [직접 알림이 처리된 것을 확인한다.]
2. [A가 `활동 재확인`을 실행한다.]
3. [B는 100점·안심, C는 활동 재확인 알림을 보여 준다.]

**발표 멘트**

> 보호자 확인 뒤 움직임이 다시 들어오면 현재 상태를 100점 ‘안심’으로 복귀시킬 수 있습니다. 피우다의 핵심은 AI에게 위험 판단을 맡기는 것이 아닙니다. 일정과 비접촉 센서 신호는 설명 가능한 규칙으로 판단하고, AI는 이해하기 쉬운 생활 안내를 담당합니다. 카메라 없이 필요한 신호만 수집하고, 사용자를 먼저 확인한 뒤 정말 필요한 순간에 보호자와 연결하는 것이 피우다의 차별점입니다. 감사합니다.

## 구현 기술 근거

| 기능 | 실제 구현 | 코드 근거 |
| --- | --- | --- |
| 반복 일정 | 요일 비트마스크로 활성 루틴 조회, 시간대가 적용된 오늘 항목 생성, `ON CONFLICT`로 중복 방지, 유예시간 뒤 미수행 전환 | `piuda/scheduler.py`의 `materialize_day()`, `refresh_missed()`, `today_tasks()`; `piuda/schema.sql`의 `task_occurrences` |
| 완료 처리 | 완료 가능한 항목만 `completed`로 바꾸고 완료 시각·메모 저장 후 점수 재평가 | `piuda/api.py`의 `complete_task()` |
| ESP32 PIR | GPIO를 250ms 주기로 읽고 상승 시 `pir_motion` 이벤트 큐 등록 | `firmware/esp32_sensor/main/piuda_sensor.c`의 `pir_task()` |
| ESP32 CSI 송신 | 같은 2.4GHz Wi-Fi 채널에서 고정 로컬 MAC으로 초당 50개 ESP-NOW 측정 패킷 전송 | `firmware/esp32_csi_transmitter/main/piuda_csi_transmitter.c`의 `csi_transmit_task()` |
| ESP32 Wi-Fi CSI | 지정 송신 MAC만 수신해 I/Q RMS 계산, 최초 250패킷 기준선, 이동 기준선·변화율·연속 임계값·중복 억제로 motion/fall 분류 | `firmware/esp32_sensor/main/piuda_sensor.c`의 `csi_receive_callback()`, `csi_analysis_task()` |
| 센서 수집 보안 | 센서별 1회성 키 발급, SHA-256 해시 저장, `X-Piuda-Sensor-Key` 검증, 이벤트 형식·신뢰도 검증 | `piuda/api.py`의 `register_sensor()`, `sensor_event()`; `piuda/auth.py`의 `authenticate_sensor()` |
| 건강 점수 | 일정·최근 움직임·CSI·야간 움직임·센서 연결 상태를 규칙별로 감점하고 0~100으로 제한; 76~100 안심, 51~75 살펴보기, 21~50 주의, 0~20 긴급 | `piuda/risk.py`의 `RISK_RULES`, `level_for_score()`, `evaluate_risk()` |
| 센서 연결 끊김 | 센서의 마지막 신호 시각, 신호가 없으면 등록 시각을 기준으로 30분 이상 상태 신호가 없을 때 일반 점수 엔진에서 25점 감점 | `piuda/risk.py`의 `SENSOR_OFFLINE_WINDOW`, `_offline_sensor_evidence()`, `evaluate_risk()` |
| 알림 중복 억제 | 같은 점수·근거는 5분 안에 평가 기록 재사용, 같은 단계 알림은 30분 안에 재생성하지 않음 | `piuda/risk.py`의 `evaluate_risk()` |
| 사용자 직접 알림 | 사설망에서만 요청 허용, 미확인 직접 요청은 30초간 재사용, 보호자 화면에서 팝업·소리·진동과 처리 시각 기록 | `piuda/api.py`의 `caregiver_alert()`; `piuda/demo.py`의 `create_caregiver_request_alert()`; `piuda/static/app.js`의 `sendCaregiverAlert()`, `notifyNewCaregiverAlert()` |
| 사용자 선확인 | 발표 상태가 `inactivity_check`이면 30초 다이얼로그, `ok/help/timeout`을 wellness API로 전송 | `piuda/static/app.js`의 `syncWellnessPrompt()`, `respondWellness()`; `piuda/api.py`의 `wellness_check()` |
| 발표 제어실 | 로컬·데모 모드에서만 12개 장면 허용, 로그인은 보존하면서 데이터 상태를 재현 | `piuda/api.py`의 `_demo_access_error()`; `piuda/demo.py`의 `trigger_demo_scenario()` |
| 보호자 동기화 | 사용자·보호자·제어실을 2초 주기로 `no-store` API 조회, 포커스 복귀 시 즉시 새로고침 | `piuda/static/app.js`의 `refreshUserSnapshot()`, `loadDashboard()`, `loadDemoStatus()` |
| 로컬 AI | 긴급·일정 질문 우선 규칙, 최근 대화와 현재 맥락을 Ollama chat에 전달, 출력 정제, 실패 시 안전 폴백 | `piuda/integrations.py`의 `_fast_feedback()`, `_recall_recent_user_message()`, `ollama_feedback()` |
| 음성 입출력 | 웹 SpeechRecognition, 브라우저 SpeechSynthesis, Pi 키오스크의 `/tts`와 gTTS 캐시·espeak fallback | `piuda/static/app.js`의 `setupVoiceInput()`, `speak()`; `piuda/tts.py`; `piuda/api.py`의 `local_tts()` |
| 보호자 인증 | PIN 해시, 세션 인증 버전, 해시된 bearer token, 로그아웃 시 토큰 폐기 | `piuda/auth.py`; `piuda/schema.sql`의 `caregivers`, `api_tokens` |
| 로컬 저장 | SQLite 외래 키, WAL, 5초 busy timeout; 일정·센서·점수·알림·대화 분리 저장 | `piuda/db.py`; `piuda/schema.sql` |
| PWA 배포 | 사용자·보호자 manifest, v20 shell 캐시, 자산별 독립 설치 캐시, `event.waitUntil` 기반 백그라운드 갱신, wheel의 아이콘·QR 패키징 | `piuda/static/service-worker.js`; `piuda/static/manifest.webmanifest`; `piuda/static/caregiver-manifest.webmanifest`; `pyproject.toml` |

## 구현 범위를 정확히 말하기

- 제어실 장면은 발표의 재현성을 위해 데이터베이스에 정해진 일정·센서·점수 상태를 넣는다. 실제 센서에서 방금 발생한 물리 이벤트라고 표현하지 않는다.
- 일반 점수 엔진은 실제 일정과 센서 데이터를 평가하지만, 30초 사용자 선확인 다이얼로그와 `/wellness-check` 연결은 현재 데모 모드 장면에 구현되어 있다. 운영 환경 전체에 자동 배포된 기능이라고 과장하지 않는다.
- `센서 연결 끊김`은 일반 `RISK_RULES`의 25점 감점 항목이다. 마지막 신호가 없는 새 센서는 등록 시각부터, 신호를 받은 센서는 `last_seen_at`부터 30분 이상 지나면 적용된다.
- CSI는 임계값 기반의 **넘어짐 의심 보조 신호**다. 의료기기 판정이나 검증되지 않은 정확도 수치를 말하지 않는다.
- 카메라는 사용하지 않지만 일정·센서·대화 기록은 Pi의 SQLite에 저장된다. “아무 데이터도 저장하지 않는다”라고 말하지 않는다.
- Ollama 모델 추론은 Pi에서 처리하지만 gTTS는 네트워크가 필요할 수 있고, 웹 SpeechRecognition도 브라우저 구현에 따라 외부 처리가 가능하다. “음성까지 완전 오프라인”이라고 말하지 않는다.
- PWA의 화면 셸은 캐시되지만 일정·센서 API 데이터에는 Pi 연결이 필요하다.
- SwiftUI 앱은 일정·대시보드·STT/TTS를 구현한다. 발표의 사용자 직접 보호자 알림은 웹/PWA 화면에서 시연한다.

## 실패 대비 플랜

| 문제 | 즉시 확인 | 발표 중 복구 | 계속 실패할 때 멘트와 대체 |
| --- | --- | --- | --- |
| 장면이 다른 화면에 반영되지 않음 | 세 화면의 연결 표시, 같은 Wi-Fi, 화면이 백그라운드인지 확인 | 2초 기다린 뒤 해당 화면을 한 번 터치하거나 새로고침 | “세 화면은 동일 REST API를 조회합니다. 지금은 준비한 캡처로 결과를 보여 드리겠습니다.” |
| 30초 뒤 보호자 알림이 안 뜸 | 사용자 팝업의 카운트다운과 보호자 화면 포커스 확인 | A가 `확인에 응답 없음` 장면을 직접 실행 | “시간상 미응답 이후 상태를 제어실에서 재현했습니다.”라고 밝히고 30점·알림을 설명 |
| 알림음이 나지 않음 | C가 발표 전에 화면을 터치해 AudioContext를 활성화했는지 확인 | 화면을 한 번 터치하고 시각적 팝업을 계속 시연 | 소리는 생략하고 큰 팝업·진동·미확인 알림 수치로 설명 |
| AI 응답이 늦음 | 서버 연결과 모델 예열 확인 | 요청을 중복 전송하지 않고 발표 설명을 계속한다. 20초 뒤에도 결과가 없으면 두 번째 기억 질문 생략 | “일정·긴급 질문은 모델을 거치지 않고 즉시 답하며, 모델 실패 시 안전 폴백을 반환합니다.”라고 설명 |
| AI 음성이 안 나옴 | `소리 켜짐`, Pi 볼륨, 네트워크 확인 | `다시 듣기`를 한 번만 누름 | 화면의 텍스트 응답으로 진행하고 gTTS·espeak fallback 구조만 설명 |
| 사용자 직접 알림이 안 뜸 | B·C의 연결 표시와 C의 로그인·화면 포커스 확인 | 2초 기다린 뒤 C를 새로고침하고 알림 목록 확인 | 제어실의 `보호자 알림 요청` 장면으로 같은 DB 알림 흐름을 재현하고 설명 |
| ESP32 실기 신호가 불안정 | heartbeat와 센서 키 확인 | 제어실의 `넘어짐 의심` 장면 사용 | “물리적 넘어짐 대신 동일 이벤트 데이터를 재현합니다.”라고 명시 |
| 서버 자체가 응답하지 않음 | `http://127.0.0.1:8080/api/v1/health` 확인 | 키오스크를 종료하고 `피우다 실행`으로 한 번만 재시작 | 재시작 시간을 넘기면 백업 영상과 구조 설명으로 발표 계속 |

## 발표 전 체크리스트

### 전날 또는 T-30분

- [ ] Pi, 제어실 기기, 보호자 스마트폰을 동일한 Wi-Fi에 연결했다.
- [ ] `CNU.local`이 두 보조 기기에서 Pi를 가리키는지 확인했다.
- [ ] Pi에서 `http://127.0.0.1:8080/api/v1/health`가 `status: ok`를 반환한다.
- [ ] 제어실에서 `http://CNU.local:8080/demo`가 열리고 12개 장면이 보인다.
- [ ] 보호자 화면을 `http://CNU.local:8080/caregiver`로 열고 PIN `3017`로 로그인했다.
- [ ] 사용자 화면의 직접 알림 버튼을 눌러 보호자 팝업·알림음·진동·확인 처리를 검증했다.
- [ ] 음성 질문을 시연한다면 Pi에 USB 마이크를 연결하고 입력 장치로 인식되는지 확인했다.
- [ ] `.venv/bin/pytest -q`가 모두 통과하는지 확인했다.
- [ ] Pi의 AI 모델이 예열되어 일반 질문에 응답하는지 확인했다.
- [ ] gTTS 재생 또는 espeak fallback과 Pi 스피커 볼륨을 확인했다.
- [ ] 두 AI 질문을 클립보드나 메모에 준비했다: `오늘 보리차를 마셨어요. 잘했죠?`, `제가 아까 무엇을 마셨죠?`.

### T-5분

- [ ] 최종 알림 테스트 뒤 Pi 앱을 다시 실행해 시연 DB를 초기화했다.
- [ ] 앱 재실행 후 보호자 PIN `3017`로 다시 로그인했다. 재실행 전 토큰은 초기화된다.
- [ ] A는 `/demo`, B는 Pi 사용자 키오스크, C는 보호자 대시보드를 전면에 열었다.
- [ ] 보호자 화면을 한 번 터치해 알림용 AudioContext를 활성화했다.
- [ ] 같은 역할의 중복 브라우저 탭을 닫아 중복 폴링·수신음을 방지했다.
- [ ] 스마트폰 자동 잠금을 끄고 배터리 절약 모드를 해제했다.
- [ ] 알림 팝업을 가릴 수 있는 다른 앱 알림을 정리했다.
- [ ] A가 8분 카운트다운 타이머와 이 문서의 타임라인을 함께 볼 수 있게 했다.
- [ ] 30초 무응답이 늦을 때 쓸 `확인에 응답 없음` 버튼과 직접 알림 장면을 A가 바로 찾을 수 있다.

### 직접 알림 직전 10초 재확인

- [ ] C가 보호자 대시보드에 로그인해 화면을 전면에 열었다.
- [ ] C가 화면을 한 번 터치해 알림음을 재생할 수 있게 했다.
- [ ] B가 사용자 화면의 `보호자에게 알림 보내기` 버튼을 누를 준비가 됐다.
- [ ] C가 팝업을 확인한 뒤 `확인했습니다`를 눌러 처리 시각을 기록한다.
